# Environment / secrets
.env
.env.*
!.env.example
.cursor/

# Python
__pycache__/
*.pyc
.venv/

# ChromaDB (replaced by Supabase pgvector — no longer used)
backend/chroma_db/

# Node
node_modules/
.next/

# LoRA adapter weights (large binary files — stored in Google Drive / HuggingFace)
ml/output/
ml/dataset.tutor.jsonl

# ML pipeline generated artifacts
ml/data/train.jsonl
ml/data/eval.jsonl
ml/data/eval_raw.jsonl
ml/audit_report.csv

# OS
.DS_Store
